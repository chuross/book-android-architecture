box: vvakame/review
build:
  steps:
    - script:
      name: make epub
      code: rake epub
  after-steps:
    - script:
      name: copy artifacts
      code: cp *.epub ${WERCKER_REPORT_ARTIFACTS_DIR}
